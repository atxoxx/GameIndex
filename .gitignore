# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.opencode/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
# Windows reserved-name artifacts
nul

# User-installed plugin artifacts bundled for offline use — hosted
# outside the repo, never committed. Anchored to the repo ROOT so the
# pattern does NOT swallow src-tauri/src/plugins/ (the sandboxed JS
# plugin runtime, which IS committed source).
/plugins/

# Deepwork state
.slim/deepwork/
.agents
.kilo
.opencode
.playwright-mcp
.scratch
.slim
.superpowers
.tmp
.vscode
build.bat
build-upload.bat
upload.bat
scripts/test-plugins.mjs
